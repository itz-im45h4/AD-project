spring.application.name=account-service
server.port=8081

# Set MONGODB_URI in your shell or IDE run configuration; do not commit credentials.
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/account_db}
app.security.jwt-secret=${JWT_SECRET:}
